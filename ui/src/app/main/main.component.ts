import { Component } from '@angular/core';

// Slice-0 placeholder shell. Eventually hosts the Clarity vertical-nav
// sidebar + header (mirroring xpmile/ui/src/app/main/) once login +
// directory + dialer modules land.
@Component({
    selector: 'app-main',
    templateUrl: './main.component.html',
    styleUrls: ['./main.component.scss'],
})
export class MainComponent {}
