// Ambient module declarations for libraries without bundled TypeScript
// types. sip.js, @clr/*, and @cds/* ship their own types — nothing to
// declare yet. Placeholder kept so xpmile-style `import 'src/common/types'`
// references continue to resolve.

export {};
