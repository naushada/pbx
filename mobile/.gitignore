# React Native / Node
node_modules/
npm-debug.log
yarn-error.log
# npm's local cache when --userconfig points it inside mobile/ (e.g. in
# CI / clean-checkout runs that don't have write access to ~/.npm).
.npm/

# Metro / build
.metro-health-check*
coverage/
dist/

# Native build output (the ios/ and android/ shells are generated by
# the React Native CLI on a dev machine — see README §Native projects)
ios/build/
ios/Pods/
ios/*.xcworkspace
android/.gradle/
android/app/build/
android/build/
*.keystore
!debug.keystore

# Detox (E2E — TDD layer M4)
artifacts/
.detoxrc.local*

# Env / secrets
.env
.env.*

# OS / editor
.DS_Store
*.log
